<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- TODO: remember to update normal values at the same time -->

    <style name="default_light_theme" parent="@android:Theme.Holo.Light" />

    <style name="default_dark_theme" parent="@android:Theme.Holo" />

    <style name="fullscreen_theme" parent="android:Theme.Holo">
        <item name="android:actionBarStyle">@style/fullscreen_action_bar</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="fullscreen_action_bar" parent="android:Widget.Holo.ActionBar">
        <item name="android:background">@color/actionbar_fullscreen</item>
    </style>

    <style name="default_dialog" parent="@android:Theme.Holo.Dialog" />

</resources>
